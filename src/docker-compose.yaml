services:
  db:
    image: postgres:15
    container_name: business_db_container
    environment:
      POSTGRES_DB: business_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432" # Зовнішній порт : Внутрішній порт